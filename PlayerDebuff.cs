using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayerDebuff : MonoBehaviour
{
    /*
    void Update()
    {
        if (Input.GetMouseButtonDown(0))
        {
            Vector2 mousePos = Camera.main.ScreenToWorldPoint(Input.mousePosition);
            Vector3 objPos = transform.position;
            Debug.Log(mousePos);
            Debug.Log(objPos);
            Collider2D[] colliders = Physics2D.OverlapBox(new Rect(mousePos.x - 0.1f, mousePos.y - 0.1f, mousePos.x + 0.1f, mousePos.y + 0.1f), 0.1f, rayPosition.transform, true);
            if (colliders.Length > 0)
            {
                Debug.Log("Clicked on: " + colliders[0].gameObject.name);
            }
        }
    }
    */
}

